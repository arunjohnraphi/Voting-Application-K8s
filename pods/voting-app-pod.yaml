apiVersion: v1
kind: Pod
metadata:
  name: vote-pod
  labels:
    app: vote
spec:
  containers:
  - name: vote-container
    image: dockersamples/examplevotingapp_vote
    ports:
      - containerPort: 80
        name: vote-port

